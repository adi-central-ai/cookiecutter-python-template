"""Version test for cookiecutter-python-template."""
import pytest


@pytest.mark.skip(reason="No version test yet.")
def test_version() -> None:
    """Check that all the version tags are in sync."""
    assert True
